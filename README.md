# saiphhaven

Static Saiph site with a Decap CMS admin panel for the Noema gallery.

After deployment, edit `admin/config.yml` and replace `YOUR_GITHUB_USERNAME/saiphhaven`
with the exact GitHub repository path.
